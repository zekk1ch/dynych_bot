import React from 'react';
import PropTypes from 'react-proptypes';

class Week extends React.Component {
    render() {
        return (
            <div className="week">
            </div>
        );
    }
}

export default Week;

Week.propTypes = {
};
