import React from 'react';
import PropTypes from 'react-proptypes';
import Slidable from '../Slidable';
import Month from './Week';

class WeekView extends React.Component {
    render() {
        return (
            <div className="view">
            </div>
        );
    }
}

export default WeekView;

WeekView.propTypes = {
};
